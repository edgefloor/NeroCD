// Code generated by NeroCD BobGen. DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package factory

import (
	"context"
	"testing"
	"time"

	"github.com/jaswdr/faker/v2"
	"github.com/lib/pq"
	"github.com/stephenafamo/bob"
	models "nerocd/internal/store/bobgen/models"
)

type RunnerMod interface {
	Apply(context.Context, *RunnerTemplate)
}

type RunnerModFunc func(context.Context, *RunnerTemplate)

func (f RunnerModFunc) Apply(ctx context.Context, n *RunnerTemplate) {
	f(ctx, n)
}

type RunnerModSlice []RunnerMod

func (mods RunnerModSlice) Apply(ctx context.Context, n *RunnerTemplate) {
	for _, f := range mods {
		f.Apply(ctx, n)
	}
}

// RunnerTemplate is an object representing the database table.
// all columns are optional and should be set by mods
type RunnerTemplate struct {
	ID              func() string
	Name            func() string
	Tags            func() pq.StringArray
	Capabilities    func() pq.StringArray
	Status          func() string
	RegisteredAt    func() time.Time
	LastHeartbeatAt func() time.Time
	TokenHash       func() string

	r runnerR
	f *Factory

	alreadyPersisted bool
}

type runnerR struct {
	RunLeases []*runnerRRunLeasesR
}

type runnerRRunLeasesR struct {
	number int
	o      *RunLeaseTemplate
}

// Apply mods to the RunnerTemplate
func (o *RunnerTemplate) Apply(ctx context.Context, mods ...RunnerMod) {
	for _, mod := range mods {
		mod.Apply(ctx, o)
	}
}

// setModelRels creates and sets the relationships on *models.Runner
// according to the relationships in the template. Nothing is inserted into the db
func (t RunnerTemplate) setModelRels(o *models.Runner) {
	if t.r.RunLeases != nil {
		rel := models.RunLeaseSlice{}
		for _, r := range t.r.RunLeases {
			related := r.o.BuildMany(r.number)
			for _, rel := range related {
				rel.RunnerID = o.ID // h2
			}
			rel = append(rel, related...)
		}
		o.R.RunLeases = rel
		o.R.Loaded.RunLeases = true
	}
}

// BuildSetter returns an *models.RunnerSetter
// this does nothing with the relationship templates
func (o RunnerTemplate) BuildSetter() *models.RunnerSetter {
	m := &models.RunnerSetter{}

	if o.ID != nil {
		val := o.ID()
		m.ID = func() *string { return &val }()
	}
	if o.Name != nil {
		val := o.Name()
		m.Name = func() *string { return &val }()
	}
	if o.Tags != nil {
		val := o.Tags()
		m.Tags = func() *pq.StringArray { return &val }()
	}
	if o.Capabilities != nil {
		val := o.Capabilities()
		m.Capabilities = func() *pq.StringArray { return &val }()
	}
	if o.Status != nil {
		val := o.Status()
		m.Status = func() *string { return &val }()
	}
	if o.RegisteredAt != nil {
		val := o.RegisteredAt()
		m.RegisteredAt = func() *time.Time { return &val }()
	}
	if o.LastHeartbeatAt != nil {
		val := o.LastHeartbeatAt()
		m.LastHeartbeatAt = func() *time.Time { return &val }()
	}
	if o.TokenHash != nil {
		val := o.TokenHash()
		m.TokenHash = func() *string { return &val }()
	}

	return m
}

// BuildManySetter returns an []*models.RunnerSetter
// this does nothing with the relationship templates
func (o RunnerTemplate) BuildManySetter(number int) []*models.RunnerSetter {
	m := make([]*models.RunnerSetter, number)

	for i := range m {
		m[i] = o.BuildSetter()
	}

	return m
}

// Build returns an *models.Runner
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use RunnerTemplate.Create
func (o RunnerTemplate) Build() *models.Runner {
	m := &models.Runner{}

	if o.ID != nil {
		m.ID = o.ID()
	}
	if o.Name != nil {
		m.Name = o.Name()
	}
	if o.Tags != nil {
		m.Tags = o.Tags()
	}
	if o.Capabilities != nil {
		m.Capabilities = o.Capabilities()
	}
	if o.Status != nil {
		m.Status = o.Status()
	}
	if o.RegisteredAt != nil {
		m.RegisteredAt = o.RegisteredAt()
	}
	if o.LastHeartbeatAt != nil {
		m.LastHeartbeatAt = o.LastHeartbeatAt()
	}
	if o.TokenHash != nil {
		m.TokenHash = o.TokenHash()
	}

	o.setModelRels(m)

	return m
}

// BuildMany returns an models.RunnerSlice
// Related objects are also created and placed in the .R field
// NOTE: Objects are not inserted into the database. Use RunnerTemplate.CreateMany
func (o RunnerTemplate) BuildMany(number int) models.RunnerSlice {
	m := make(models.RunnerSlice, number)

	for i := range m {
		m[i] = o.Build()
	}

	return m
}

func ensureCreatableRunner(m *models.RunnerSetter) {
	if !(m.ID != nil) {
		val := random_string(nil)
		m.ID = func() *string { return &val }()
	}
	if !(m.Name != nil) {
		val := random_string(nil)
		m.Name = func() *string { return &val }()
	}
	if !(m.Status != nil) {
		val := random_string(nil)
		m.Status = func() *string { return &val }()
	}
}

// insertOptRels creates and inserts any optional the relationships on *models.Runner
// according to the relationships in the template.
// any required relationship should have already exist on the model
func (o *RunnerTemplate) insertOptRels(ctx context.Context, exec bob.Executor, m *models.Runner) error {
	var err error

	isRunLeasesDone, _ := runnerRelRunLeasesCtx.Value(ctx)
	if !isRunLeasesDone && o.r.RunLeases != nil {
		ctx = runnerRelRunLeasesCtx.WithValue(ctx, true)
		for _, r := range o.r.RunLeases {
			if r.o.alreadyPersisted {
				m.R.RunLeases = append(m.R.RunLeases, r.o.Build())
			} else {
				rel0, err := r.o.CreateMany(ctx, exec, r.number)
				if err != nil {
					return err
				}

				err = m.AttachRunLeases(ctx, exec, rel0...)
				if err != nil {
					return err
				}
			}
		}
	}

	return err
}

// Create builds a runner and inserts it into the database
// Relations objects are also inserted and placed in the .R field
func (o *RunnerTemplate) Create(ctx context.Context, exec bob.Executor) (*models.Runner, error) {
	var err error
	opt := o.BuildSetter()
	ensureCreatableRunner(opt)

	// Retrieve ancestor models from context to avoid duplicate parent creation.
	// Parents are keyed by "parent_table:child_table:child_rel_name".
	// This works regardless of NoBackReferencing since it only uses child-side metadata.
	mInCreation, _ := modelsInCreationCtx.Value(ctx)

	m, err := models.Runners.Insert(opt).One(ctx, exec)
	if err != nil {
		return nil, err
	}

	// Store this model in context for child creates.
	// Key format: "parent_table:child_table:child_rel_name" where child_rel_name is the FK name.
	// We store an entry for every relationship pointing TO this table (where this table is the parent).
	// This works regardless of NoBackReferencing since keys use child-side metadata.
	newMInCreation := make(map[string]any, len(mInCreation)+1)
	for k, v := range mInCreation {
		newMInCreation[k] = v
	}
	newMInCreation["runners:run_leases:run_leases.run_leases_runner_id_fkey"] = m

	ctx = modelsInCreationCtx.WithValue(ctx, newMInCreation)

	if err := o.insertOptRels(ctx, exec, m); err != nil {
		return nil, err
	}
	return m, err
}

// MustCreate builds a runner and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o *RunnerTemplate) MustCreate(ctx context.Context, exec bob.Executor) *models.Runner {
	m, err := o.Create(ctx, exec)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateOrFail builds a runner and inserts it into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o *RunnerTemplate) CreateOrFail(ctx context.Context, tb testing.TB, exec bob.Executor) *models.Runner {
	tb.Helper()
	m, err := o.Create(ctx, exec)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// CreateMany builds multiple runners and inserts them into the database
// Relations objects are also inserted and placed in the .R field
func (o RunnerTemplate) CreateMany(ctx context.Context, exec bob.Executor, number int) (models.RunnerSlice, error) {
	var err error
	m := make(models.RunnerSlice, number)

	for i := range m {
		m[i], err = o.Create(ctx, exec)
		if err != nil {
			return nil, err
		}
	}

	return m, nil
}

// MustCreateMany builds multiple runners and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// panics if an error occurs
func (o RunnerTemplate) MustCreateMany(ctx context.Context, exec bob.Executor, number int) models.RunnerSlice {
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		panic(err)
	}
	return m
}

// CreateManyOrFail builds multiple runners and inserts them into the database
// Relations objects are also inserted and placed in the .R field
// It calls `tb.Fatal(err)` on the test/benchmark if an error occurs
func (o RunnerTemplate) CreateManyOrFail(ctx context.Context, tb testing.TB, exec bob.Executor, number int) models.RunnerSlice {
	tb.Helper()
	m, err := o.CreateMany(ctx, exec, number)
	if err != nil {
		tb.Fatal(err)
		return nil
	}
	return m
}

// Runner has methods that act as mods for the RunnerTemplate
var RunnerMods runnerMods

type runnerMods struct{}

func (m runnerMods) RandomizeAllColumns(f *faker.Faker) RunnerMod {
	return RunnerModSlice{
		RunnerMods.RandomID(f),
		RunnerMods.RandomName(f),
		RunnerMods.RandomTags(f),
		RunnerMods.RandomCapabilities(f),
		RunnerMods.RandomStatus(f),
		RunnerMods.RandomRegisteredAt(f),
		RunnerMods.RandomLastHeartbeatAt(f),
		RunnerMods.RandomTokenHash(f),
	}
}

// Set the model columns to this value
func (m runnerMods) ID(val string) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.ID = func() string { return val }
	})
}

// Set the Column from the function
func (m runnerMods) IDFunc(f func() string) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.ID = f
	})
}

// Clear any values for the column
func (m runnerMods) UnsetID() RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.ID = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m runnerMods) RandomID(f *faker.Faker) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.ID = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m runnerMods) Name(val string) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Name = func() string { return val }
	})
}

// Set the Column from the function
func (m runnerMods) NameFunc(f func() string) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Name = f
	})
}

// Clear any values for the column
func (m runnerMods) UnsetName() RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Name = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m runnerMods) RandomName(f *faker.Faker) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Name = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m runnerMods) Tags(val pq.StringArray) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Tags = func() pq.StringArray { return val }
	})
}

// Set the Column from the function
func (m runnerMods) TagsFunc(f func() pq.StringArray) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Tags = f
	})
}

// Clear any values for the column
func (m runnerMods) UnsetTags() RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Tags = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m runnerMods) RandomTags(f *faker.Faker) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Tags = func() pq.StringArray {
			return random_pq_StringArray(f)
		}
	})
}

// Set the model columns to this value
func (m runnerMods) Capabilities(val pq.StringArray) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Capabilities = func() pq.StringArray { return val }
	})
}

// Set the Column from the function
func (m runnerMods) CapabilitiesFunc(f func() pq.StringArray) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Capabilities = f
	})
}

// Clear any values for the column
func (m runnerMods) UnsetCapabilities() RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Capabilities = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m runnerMods) RandomCapabilities(f *faker.Faker) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Capabilities = func() pq.StringArray {
			return random_pq_StringArray(f)
		}
	})
}

// Set the model columns to this value
func (m runnerMods) Status(val string) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Status = func() string { return val }
	})
}

// Set the Column from the function
func (m runnerMods) StatusFunc(f func() string) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Status = f
	})
}

// Clear any values for the column
func (m runnerMods) UnsetStatus() RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Status = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m runnerMods) RandomStatus(f *faker.Faker) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.Status = func() string {
			return random_string(f)
		}
	})
}

// Set the model columns to this value
func (m runnerMods) RegisteredAt(val time.Time) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.RegisteredAt = func() time.Time { return val }
	})
}

// Set the Column from the function
func (m runnerMods) RegisteredAtFunc(f func() time.Time) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.RegisteredAt = f
	})
}

// Clear any values for the column
func (m runnerMods) UnsetRegisteredAt() RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.RegisteredAt = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m runnerMods) RandomRegisteredAt(f *faker.Faker) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.RegisteredAt = func() time.Time {
			return random_time_Time(f)
		}
	})
}

// Set the model columns to this value
func (m runnerMods) LastHeartbeatAt(val time.Time) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.LastHeartbeatAt = func() time.Time { return val }
	})
}

// Set the Column from the function
func (m runnerMods) LastHeartbeatAtFunc(f func() time.Time) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.LastHeartbeatAt = f
	})
}

// Clear any values for the column
func (m runnerMods) UnsetLastHeartbeatAt() RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.LastHeartbeatAt = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m runnerMods) RandomLastHeartbeatAt(f *faker.Faker) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.LastHeartbeatAt = func() time.Time {
			return random_time_Time(f)
		}
	})
}

// Set the model columns to this value
func (m runnerMods) TokenHash(val string) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.TokenHash = func() string { return val }
	})
}

// Set the Column from the function
func (m runnerMods) TokenHashFunc(f func() string) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.TokenHash = f
	})
}

// Clear any values for the column
func (m runnerMods) UnsetTokenHash() RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.TokenHash = nil
	})
}

// Generates a random value for the column using the given faker
// if faker is nil, a default faker is used
func (m runnerMods) RandomTokenHash(f *faker.Faker) RunnerMod {
	return RunnerModFunc(func(_ context.Context, o *RunnerTemplate) {
		o.TokenHash = func() string {
			return random_string(f)
		}
	})
}

func (m runnerMods) WithParentsCascading() RunnerMod {
	return RunnerModFunc(func(ctx context.Context, o *RunnerTemplate) {
		if isDone, _ := runnerWithParentsCascadingCtx.Value(ctx); isDone {
			return
		}
		ctx = runnerWithParentsCascadingCtx.WithValue(ctx, true)
	})
}

func (m runnerMods) WithRunLeases(number int, related *RunLeaseTemplate) RunnerMod {
	return RunnerModFunc(func(ctx context.Context, o *RunnerTemplate) {
		o.r.RunLeases = []*runnerRRunLeasesR{{
			number: number,
			o:      related,
		}}
	})
}

func (m runnerMods) WithNewRunLeases(number int, mods ...RunLeaseMod) RunnerMod {
	return RunnerModFunc(func(ctx context.Context, o *RunnerTemplate) {
		related := o.f.NewRunLeaseWithContext(ctx, mods...)
		m.WithRunLeases(number, related).Apply(ctx, o)
	})
}

func (m runnerMods) AddRunLeases(number int, related *RunLeaseTemplate) RunnerMod {
	return RunnerModFunc(func(ctx context.Context, o *RunnerTemplate) {
		o.r.RunLeases = append(o.r.RunLeases, &runnerRRunLeasesR{
			number: number,
			o:      related,
		})
	})
}

func (m runnerMods) AddNewRunLeases(number int, mods ...RunLeaseMod) RunnerMod {
	return RunnerModFunc(func(ctx context.Context, o *RunnerTemplate) {
		related := o.f.NewRunLeaseWithContext(ctx, mods...)
		m.AddRunLeases(number, related).Apply(ctx, o)
	})
}

func (m runnerMods) AddExistingRunLeases(existingModels ...*models.RunLease) RunnerMod {
	return RunnerModFunc(func(ctx context.Context, o *RunnerTemplate) {
		for _, em := range existingModels {
			o.r.RunLeases = append(o.r.RunLeases, &runnerRRunLeasesR{
				o: o.f.fromExistingRunLease(ctx, em),
			})
		}
	})
}

func (m runnerMods) WithoutRunLeases() RunnerMod {
	return RunnerModFunc(func(ctx context.Context, o *RunnerTemplate) {
		o.r.RunLeases = nil
	})
}
